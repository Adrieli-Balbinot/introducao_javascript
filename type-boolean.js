const value1 = true;
const value2 = false;

// Thursty / False -> Variavel vazia ou com o valor de 0, se submeter 
// a um texto lógico, se tornarao um tipo boolean (thrusty/false)

console.log(value1 == value1);

if (value1) 
    console.log('Passou aqui!')

const resultado = value1 ? 'Verdadeiro' : 'Falso';
